# ── PDF extraction ────────────────────────────────────────────────────────────
pdfplumber>=0.11.0          # primary PDF text + table extractor
pymupdf>=1.24.0             # fallback extractor (imports as fitz)

# ── Excel output ──────────────────────────────────────────────────────────────
openpyxl>=3.1.0             # builds formatted .xlsx workbooks
pandas>=2.0.0               # used in sustainability_extractor for data handling

# ── RAG — embeddings ──────────────────────────────────────────────────────────
sentence-transformers>=3.0.0   # local embeddings, model: all-MiniLM-L6-v2

# ── RAG — vector store ────────────────────────────────────────────────────────
chromadb>=0.5.0             # local vector DB (dev); swap to qdrant-client for prod

# ── LLM — OpenRouter (OpenAI-compatible API) ──────────────────────────────────
openai>=1.30.0              # SDK works with OpenRouter via base_url override

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv>=1.0.0        # loads OPENROUTER_API_KEY from .env file

# ── API server (when building FastAPI backend) ────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0             # ASGI server to run FastAPI
python-multipart>=0.0.9     # required for file uploads in FastAPI
